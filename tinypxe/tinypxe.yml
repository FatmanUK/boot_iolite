---
- name: 'Linux'
  hosts: 'all'
  connection: 'local'
  run_once: yes
  gather_facts: yes
  vars:
    workdir: 'source/kernel'
    outputdir: 'output/boot'
    dirs:
      - '{{ workdir }}'
      - '{{ outputdir }}'
  tags:
    - 'never'
    - 'linux'
    - 'kernel'
  roles:
    - 'kernel_build'

- name: 'busybox'
  hosts: 'all'
  connection: 'local'
  run_once: yes
  gather_facts: yes
  vars:
    workdir: 'source/busybox'
    outputdir: 'source/initramfs'
    dirs:
      - '{{ workdir }}'
      - '{{ outputdir }}/bin'
      - '{{ outputdir }}/sbin'
      - '{{ outputdir }}/usr/bin'
      - '{{ outputdir }}/usr/sbin'
  tags:
    - 'never'
    - 'busybox'
    - 'shell'
  roles:
    - 'busybox_build'

- name: 'runit'
  hosts: 'all'
  connection: 'local'
  run_once: yes
  gather_facts: yes
  vars:
    workdir: 'source/runit'
    outputdir: 'source/initramfs'
    dirs:
      - '{{ workdir }}'
      - '{{ outputdir }}/bin'
      - '{{ outputdir }}/etc/runit'
      - '{{ outputdir }}/etc/sv'
      - '{{ outputdir }}/var/service'
  tags:
    - 'never'
    - 'runit'
    - 'init'
  roles:
    - 'runit_build'
    - 'runit_services_create'

- name: 'initramfs'
  hosts: 'all'
  connection: 'local'
  run_once: yes
  gather_facts: no
  vars:
    workdir: 'source/initramfs'
    outputdir: 'output/boot'
    dirs:
      - '{{ workdir }}/dev'
      - '{{ workdir }}/proc'
      - '{{ workdir }}/sys'
      - '{{ workdir }}/root'
      - '{{ workdir }}/sbin'
      - '{{ workdir }}/usr/bin'
      - '{{ workdir }}/usr/sbin'
      - '{{ outputdir }}'
      - '{{ outputdir }}/isolinux'
  tags:
    - 'initramfs'
    - 'initrd'
  roles:
    - 'initramfs_build'

- name: 'mkisofs'
  hosts: 'all'
  connection: 'local'
  run_once: yes
  gather_facts: no
  vars:
    workdir: 'source/initramfs'
    outputdir: 'output/boot'
    dirs:
      - '{{ workdir }}'
      - '{{ outputdir }}'
  tags:
    - 'mkisofs'
    - 'iso'
  roles:
    - 'iso_build'
...
# vim: set filetype=yaml
